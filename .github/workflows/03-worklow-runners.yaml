name: 03 - Workflow Runners

on: push

jobs:
  ubuntu-echo:
    runs-on: ubuntut-latest
    steps:
      - name: Show OS
        run: |
          echo "This job is running on the Ubuntu runner"
          echo "Runner OS: $RUNNER_OS"